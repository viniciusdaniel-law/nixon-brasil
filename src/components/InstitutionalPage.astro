---
import type { CollectionEntry } from 'astro:content';
import { render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

interface Props {
  entry: CollectionEntry<'paginas'>;
}

const { entry } = Astro.props;
const { Content, headings } = await render(entry);
const sections = headings.filter((heading) => heading.depth === 2);
---
<BaseLayout title={entry.data.title} description={entry.data.description}>
  <section class="page-hero">
    <div class="shell">
      <p class="kicker light">{entry.data.eyebrow}</p>
      <h1>{entry.data.title}</h1>
      <p>{entry.data.lede}</p>
    </div>
  </section>
  <section class="section white">
    <div class="shell prose-shell">
      {sections.length > 0 && (
        <nav class="prose-nav" aria-label="Nesta página">
          {sections.map((heading) => <a href={`#${heading.slug}`}>{heading.text}</a>)}
        </nav>
      )}
      <article class="prose institutional-prose">
        <Content />
      </article>
    </div>
  </section>
</BaseLayout>
