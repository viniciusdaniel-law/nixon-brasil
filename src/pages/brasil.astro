---
import { getEntry } from 'astro:content';
import InstitutionalPage from '../components/InstitutionalPage.astro';

const entry = await getEntry('paginas', 'brasil');
if (!entry || entry.data.draft) throw new Error('Página sobre o Brasil indisponível.');
---
<InstitutionalPage entry={entry} />
