---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { formatDate } from '../../lib/dates';

const base = import.meta.env.BASE_URL;
const articles = (await getCollection('artigos', ({ data }) => !data.draft))
  .sort((a, b) => (
    b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf()
    || a.id.localeCompare(b.id, 'pt-BR')
  ));
---
<BaseLayout title="Artigos" description="Ensaios, dossiês e traduções do Nixon Brasil.">
  <section class="page-hero light">
    <div class="shell"><p class="kicker">Publicação editorial</p><h1>Artigos</h1><p>Biografia, Presidência, política externa, Brasil, discursos e fontes primárias.</p></div>
  </section>
  <section class="section">
    <div class="shell cards">
      {articles.map((article) => {
        const raw = article.data.cover ?? '';
        const cover = raw.startsWith('http') ? raw : raw ? `${base}${raw.replace(/^\//, '')}` : '';
        return (
        <a class="card" href={`${base}artigos/${article.id}/`}>
          {cover
            ? <img src={cover} alt={article.data.coverAlt ?? ''} width="640" height="360" loading="lazy" decoding="async" />
            : <div class="card-cover-fallback" aria-hidden="true">{article.data.category}</div>}
          <div class="card-body">
            <span class="card-meta">{article.data.category} · {formatDate(article.data.publishedAt)}</span>
            <h2>{article.data.title}</h2>
            <p>{article.data.description}</p>
          </div>
        </a>
        );
      })}
    </div>
  </section>
</BaseLayout>
