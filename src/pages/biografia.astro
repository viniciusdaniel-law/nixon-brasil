---
import { getEntry } from 'astro:content';
import InstitutionalPage from '../components/InstitutionalPage.astro';

const entry = await getEntry('paginas', 'biografia');
if (!entry || entry.data.draft) throw new Error('Biografia indisponível.');
---
<InstitutionalPage entry={entry} />
