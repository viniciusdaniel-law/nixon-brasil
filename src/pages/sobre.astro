---
import { getEntry } from 'astro:content';
import InstitutionalPage from '../components/InstitutionalPage.astro';

const entry = await getEntry('paginas', 'sobre');
if (!entry || entry.data.draft) throw new Error('Página institucional Sobre indisponível.');
---
<InstitutionalPage entry={entry} />
